{
  "name": "flipcrm-desktop-standalone",
  "version": "0.1.0",
  "private": true,
  "description": "FlipCRM — standalone private desktop app. Self-contained: no website link, no login, local data.",
  "scripts": {
    "setup": "npm --prefix app install && npm --prefix desktop install",
    "dev": "npm --prefix desktop run dev:all",
    "build": "npm --prefix desktop run build:exe"
  }
}
