# React 19 + Electron toolchain: allow installs to proceed even when a
# transitive dependency still advertises an older React peer range.
# Keeps `npm install` a single, friction-free command for a non-technical user.
legacy-peer-deps=true
